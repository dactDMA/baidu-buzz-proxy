# Repository instructions

- All code comments must use ASCII characters.
- Keep secrets out of source control.
- Use Python 3.12 and the project virtual environment.
- Run tests and lint checks before committing.
